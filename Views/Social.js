import React from "react";
import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableHighlight,
} from "react-native";

export default function Social() {
  return (
    <>

    </>
  );
}

const styles = StyleSheet.create({
  imageContainer: {
    marginTop: 40,
    height: "40%",
    width: "80%",
    borderRadius: 100,
  },
});
